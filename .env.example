# Base URL of the real backend API (e.g. http://localhost:3000).
# Leave unset/empty to use the built-in in-browser mock backend instead —
# no real backend is required to run the app locally.
VITE_API_BASE_URL=
